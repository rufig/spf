\ Apr.2007  (список необходимостей)

Диагностика ошибок
  -- понятый xpath точки сбоя
   ( то бишь, выводить с атрибутами name, match, id, и т.п.);
  -- контроль неизменности CSP, STATE и M (хотя бы на уровне корневого узла)
     (хотя, их только хаком или глюком можно вывести из баланса).
  [сейчас после сбоя контекст остается нетронутым, поэтому доступен для изучения 
   путем forthml-support:: NodeName  GetAttribute и т.п. ]

Корректное восстановление после исключений при трансляции

Нативное определение r:for-each (core.union.xml) сделано на очень уж низком уровне... как бы это разрулить.

Трансляция XML-документа, заданного текстом ( c-addr u ).

Трансляция XML-фрагмента, заданного текстом ( c-addr u )
  как быть с xmlns? (то бишь, надо иметь какой-то дефолтный контекст).

Трансляция xml-потока (через SAX-подобный интерфейс)
  хорошо бы в виде сопроцессов, с ленивым созданием xml-дерева и трансляцией.

Группировка правил в r:transform и управление текущим набором, формализация.

Перевод процесса сборки на XProc (вместо батников).

Оптимизация, созданием общего каталога пространств имен.

Правильная работа при вложенном r:m.
  -- пока не востребованно

Поддержка @test ( test-before | test-after) для r:rule (? а надо ли)
  (как аналог mode в XSL, или сам его как есть).


Хранилище текущего документа, для создания CARBONE-копий строк, и т.п.
  [сейчас `abc в контексте forthml дает строку без 0x0 в конце].


Слово NAMING не предупреждает при переопределении имен.

\ $Id$
